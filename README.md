# Frontend (Vite + React)

Install:
```
cd frontend
npm install
```

Run (dev):
```
npm run dev
```

The frontend expects backend at `http://localhost:4000` by default. You can customize by adding `.env` file:

```
VITE_API_BASE=http://localhost:4000
```
